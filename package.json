{
  "name": "personalfight",
  "version": "1.0.0",
  "private": true,
  "description": "Site institucional estático do PersonalFight",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5501 --open",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 5501"
  },
  "devDependencies": {
    "vite": "8.2.2"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
